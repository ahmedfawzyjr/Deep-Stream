apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: deepstream
spec:
  replicas: 3
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
    spec:
      containers:
        - name: gateway
          image: ghcr.io/ahmedfawzyjr/deepstream-gateway:latest
          ports:
            - containerPort: 50051
              name: grpc
            - containerPort: 8080
              name: http-admin
          env:
            - name: GRPC_PORT
              value: "50051"
            - name: HTTP_PORT
              value: "8080"
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: deepstream-config
                  key: kafka.brokers
            - name: REDIS_ADDR
              valueFrom:
                configMapKeyRef:
                  name: deepstream-config
                  key: redis.addr
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: deepstream-secrets
                  key: jwt.secret
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: deepstream
spec:
  selector:
    app: gateway-service
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: http-admin
      port: 8080
      targetPort: 8080
  type: ClusterIP
