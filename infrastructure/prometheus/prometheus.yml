global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'deepkick-go-api'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['api:8080'] # When running in Docker network, api runs on port 8080
